//ЗАГЛУШКА


import SwiftUI

struct FavoritesView: View {
    var body: some View {
        NavigationStack {
            Text("Избранные рецепты появятся здесь")
                .font(.title)
                .padding()
        }
    }
}
