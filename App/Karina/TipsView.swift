
//ЗАГЛУШКА

import SwiftUI

struct TipsView: View {
    var body: some View {
        NavigationStack {
            Text("Советы по приготовлению блюд")
                .font(.title)
                .padding()
        }
    }
}
